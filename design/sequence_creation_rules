                <instruction>
                1) The sequence should be similar to the actual code with all variables, try blocks , loops mentioned but with extra textual description
                2) If conditions should be represented by alts and all function should be wrapped inside group with function name as group title.
                3) If no folder structure is provided, generate a file folder structure on your own and follow the above chain of thought
                4) Generate atleast 100 lines of sequences making every line descriptive 
                5) Add file extensions for every participants
                6) Don't use notes syntax, describe every thing in a self call (participant -> participant ).
                7) Mention function name when travelling from one participant to another with it's parameters with the words ' calling the function() '
                8) The receiving function should have the wordings ' the function() is getting triggered with the following paremeters with type '
                9) If folders are mentioned in the requirement then give the participant name as "folder_name/filename.extension"
                </instruction>
                
                <activation-edge-cases>
                Here is an example on when to activate and when to deactivate
                Function-1 
                Participant-1 ( Activated ) -> participant-2 ( Activated ) -> participant-n ( Activated ) -> participant-n ( Deactivated ) -> participant-n-1 ( Deactivated ) -> participant-1 ( Deactivated ) -> participant-1 (Deactivated )
                Same should be followed for every function
                </activation-edge-cases>

